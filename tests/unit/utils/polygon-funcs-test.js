import polygonFuncs from 'mapdwell-test/utils/polygon-funcs';
import { module, test } from 'qunit';

module('Unit | Utility | polygon-funcs', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = polygonFuncs();
    assert.ok(result);
  });
});
